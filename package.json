{
  "name": "simple-queue",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "LexxXell",
  "license": "MIT",
  "scripts": {
    "compile": "yarn tsc",
    "start": "node dist/index.js",
    "dev": "yarn compile && yarn start",
    "migrate": "yarn compile && node dist/migrate.js",
    "docker-compose-up": "yarn compile && docker-compose up",
    "docker-compose-up-d": "yarn compile && docker-compose up -d",
    "docker-compose-down": "yarn compile && docker-compose down",
    "docker-compose-down-v": "yarn compile && docker-compose down -v"
  },
  "devDependencies": {
    "@types/colors": "^1.2.1",
    "@types/express": "^4.17.21",
    "@types/node": "^20.9.0",
    "@types/pdfkit": "^0.13.2",
    "@types/pg": "^8.10.9",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pdfkit": "^0.14.0",
    "pg": "^8.11.3"
  }
}
